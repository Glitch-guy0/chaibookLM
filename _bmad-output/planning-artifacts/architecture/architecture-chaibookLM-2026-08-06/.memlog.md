---
scope: chaibookLM v0.1 — NotebookLM-style research workspace
purpose: Consistency contract for independently-built units (bounded contexts, epics/stories) so they don't diverge
altitude: feature
updated: 2026-08-09T10:45
---

- (review-fix) Reviewer-gate fixes applied to spine (finalize pass): AD-13 web search approval-gated (never unprompted; fetched pages re-enter via SourceIndexer against limits); AD-14 delete cascade order fixed Qdrant→Filebase→Neon, owned by ingestion, idempotent + in-flight-tombstone guard; AD-15 Qdrant hosting is a config choice (one adapter, env-driven), not a code fork; AD-6 chunkId = deterministic hash(sourceId+position) for idempotent QStash retries; AD-9 turn defined (user+assistant, fetch re-answer counts) + resolved-citation snapshot carve-out (chunkId+sourceId+span) so old messages render chips under AD-1; AD-10 caps enumerated (10 notebooks/user, 10 sources/notebook, 30 sources/user, 5MB/source, 1-week TTL) + callback never bumps counts; AD-12 trigger defined (env request-rate threshold); TS pinned 7.x (7.0.2); QStash ~1MB cap → callback passes sourceId reference, not body; duckduckgo dropped in tech-stack.md:23 + addendum.md:28; mermaid quoted at deployment diagram; FR-10/11/12/14 explicitly deferred to UX spec (binds trimmed to FR-1..9+13).
- (decision) User feedback on deferred items (feedback.md #1-17) applied: chunking = 500 chars with 25% overlap for text; heading-aware splitting for web sources (which arrive as markdown from jina Reader r.jina.ai); topK changed 8→5 (minScore stays 0.30); embeddings get their own env vars EMBEDDING_BASE_URL/EMBEDDING_API_KEY/EMBEDDING_MODEL independent of LLM; streaming is ON — new AD-16, shikigami events streamed to client, not async-buffered; confirmed no Qdrant backups (AD-2), no TTL sweep (AD-11), standalone search surface still deferred. jina confirmed to have 2 endpoints (r.jina.ai Reader URL→markdown, s.jina.ai Search SERP→markdown top-5).
- (assumption) 
- (decision) Qdrant loss is NOT rebuilt by replaying ingestion; approved recovery = delete affected users' resource files from Filebase + surface error to those users. Binds: recovery policy, no rebuild path. Prevents: unbounded re-index cost/uncertainty after vector-store loss.
- (decision) shikigami SDK is tightly coupled into the application (approved decision) — NOT behind a port/interface, unlike storage/vector/LLM/search. Binds: app code depends directly on @glitch-guy0/shikigami. Prevents: false abstraction over the answer runtime.
- (decision) backend/ is a separate top-level service tree (not nested inside Next.js) while remaining a single-deployable modular monolith. Binds: folder structure only, not behavior. Prevents: structural refactor when extracting the service later.
- (decision) Custom template set molded to shikigami interfaces (Kairo templates are placeholders, not used): VectorStoreMemoryStrategy (MemoryStrategy), GroundedAnswerReasoningStrategy (ReasoningStrategy), NotebookSession (Session port), WebSearchTool (Tool, search impl = jina), plus app-level SourceIndexer (QStash job, single writer of chunks), EmbeddingService (shared by ingestion+retrieval) and CitationMapper. Binds: template inventory + interface conformance. Prevents: divergent ad-hoc strategy implementations across stories.
- (decision) Invariant 1 (single writer for chunk lifecycle): Qdrant chunk documents carry sourceId as a payload field; source removal = filtered delete on sourceId (delete every document matching that sourceId). Only the removal flow issues it; no other code writes to the chunk collection. Binds: deletion mechanism + single-writer. Prevents: orphaned vectors and divergent delete paths.
- (decision) Invariant 2 (one embedding model, both sides): EmbeddingService is the only caller of the embeddings endpoint, shared by SourceIndexer (chunk vectors) and VectorStoreMemoryStrategy (query vectors); model fixed per environment; a model change is a deliberate re-index event. Binds: embedding consistency. Prevents: mismatched vector spaces making retrieval silently return garbage.
- (decision) Invariant 3 (shared chunk shape): one shared kernel {chunkId, sourceId, notebookId, span{start,end}, position, text} + vector, written by ingestion, read by retrieval, resolved by CitationMapper; splitter is deterministic and span-preserving (powers FR-8 highlight). Binds: chunk data contract. Prevents: writer/reader/citation divergence.
- (decision) Citation contract: retrieved chunks enter context keyed by chunkId; GroundedAnswerReasoningStrategy emits per-sentence inline markers referencing only given chunkIds; CitationMapper (app-level) validates every marker against the retrieved set, drops unknown/stale, maps survivors to chips + span highlight. Refusal is structural: no retrieval above minScore = no chunkIds in context = unanswerable. Binds: citation flow + validation. Prevents: hallucinated/stale citations rendering.
- (decision) Retrieval params (FR-7 acceptance-run testable): topK = 8 chunks into context; minScore = 0.30 absolute cosine threshold for the refusal gate. Binds: retrieval default. Prevents: ad-hoc per-story tuning.
- (decision) Chat history window: exactly the last 7 user+assistant turns are fed into context per turn (sliding window). Chat list UI loads the most recent 7 on open and pulls the next 7 upward on scroll (pagination by 7). Full history persists in Neon; only the window is re-fed. Binds: session window + chat pagination. Prevents: unbounded per-turn token cost and full-history loads.
- (decision) L1 counters have one owner: a limits concern stores per-user/per-notebook counts in Neon and is the only place counts are read/bumped; write boundaries (create notebook, add source, upload) all route through it. Binds: count ownership. Prevents: divergent limit math across stories.
- (decision) L2 cap check-and-increment is atomic (single Postgres transaction per operation). Binds: limit enforcement. Prevents: two rapid submissions racing past a cap.
- (decision) L3 notebook TTL is enforced lazily: expiry check on dashboard load + notebook open deletes expired notebooks and informs the user (matches FR-1 wording); no cron in v0.1; a daily sweep deferred unless stale-expired notebooks become a real problem. Binds: TTL mechanism. Prevents: scheduler/infra cost for a low-frequency event.
- (decision) Operational posture: zero Qdrant backup posture for v0.1 (builder-only). No snapshot/backup requirement, no loss alerting. On Qdrant loss the approved recovery fires: delete affected users' Filebase resources entirely + surface error. Binds: ops checklist. Prevents: cost/complexity of backup infra nobody reads in v0.1.
