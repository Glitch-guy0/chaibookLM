---
epic: 2
date: "09-07-2026 12:47"
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 2: Multi-Modal Ingestion Pipeline & Durable Background Orchestration

## Epic Summary

- **Epic:** Epic 2 — Multi-Modal Ingestion Pipeline & Durable Background Orchestration
- **Status:** Complete (6/6 stories finished)
- **Completed Stories:**
  - `2-1-add-source-modal-client-side-file-validation`
  - `2-2-inngest-durable-orchestration-concurrency-failure-isolation`
  - `2-3-multi-modal-content-extractors-markdown-normalization`
  - `2-4-chunking-local-remote-vector-embeddings-qdrant-upsert`
  - `2-5-source-management-ui-real-time-status-telemetry`
  - `2-6-file-upload-ingestion-operational-telemetry`
- **Pending Stories:** None (`pending_stories: []`)
- **Evidence Inventory:**
  - Epic Spec: `_bmad-output/planning-artifacts/epics.md` (Epic 2, lines 242-338)
  - Implementation Artifacts: Story files 2-1 through 2-6 under `_bmad-output/implementation-artifacts/`
  - Build Verification Log: `_bmad-output/implementation-artifacts/bmad-build-auto-result-epic-2.md`
  - Sprint Status: `_bmad-output/implementation-artifacts/sprint-status.yaml`
  - Unit Test Execution: 24 test suites, 128 tests passing (Vitest suite)

## Findings

### Aggregate Views

1. **Architecture Delta** (Checked: full Epic 2 change set)
   - *Observation:* Clean addition of multi-modal ingestion pipeline layer (`backend/src/contexts/ingestion/extractors/`, `backend/src/contexts/ingestion/failure-handler.ts`, `app/api/inngest/functions/source-ingest.ts`). Clean separation between intake UI, API routes, durable background orchestration, vector indexers, and operational telemetry.
   - *Disposition:* Accept as-is. Architecture strictly complies with `ARCHITECTURE-SPINE.md`.

2. **Duplication Map** (Checked: extractors and error handling)
   - *Observation:* Common timestamp anchor parsing (`<!-- time: mm:ss -->`) across SRT, VTT, and YouTube transcript extractors consolidated cleanly in `subtitle-extractor.ts` and `youtube-extractor.ts`. Failure cleanup in `failure-handler.ts` is re-used by both Inngest `onFailure` step and `IngestionService` exception handlers.
   - *Disposition:* Accept as-is. Dry implementation across extraction modalities.

3. **God-Class / Size Growth** (Checked: file churn metrics)
   - *Observation:* Largest modified files remain compact and well-structured: `backend/src/adapters/neon/index.ts` (348 lines), `backend/src/contexts/ingestion/index.ts` (196 lines), `components/sources/upload-dialog.tsx` (218 lines).
   - *Disposition:* Accept as-is. No god-class anti-patterns detected.

4. **Pattern Divergence** (Checked: codebase conventions)
   - *Observation:* Tactile Neo-Brutalist UI styling (`#FFE500` indexing border, orbital shadow spin, `#FF3333` error state) follows design tokens from `DESIGN.md`. Non-blocking telemetry uses standard async database inserts (< 15ms overhead).
   - *Disposition:* Accept as-is. Fully consistent with project standards.

5. **Spec-to-Implementation Reconciliation** (Checked: declared criteria vs code)
   - *Observation:*
     - Story 2.1: 5-tab intake modal (`Text`, `Web URL`, `PDF`, `Transcript`, `YouTube`), client file size limits (PDF ≤ 10MB, Transcript ≤ 5MB), 10-source notebook cap warning, and `POST /api/sources` HTTP 201 dispatch verified.
     - Story 2.2: Inngest `fnSourceIngest` concurrency (`limit: 2` per user), 3 exponential retries, and isolated `onFailure` hook verified.
     - Story 2.3: PDF page anchors (`<!-- page: N -->`), subtitle/YouTube time anchors (`<!-- time: mm:ss -->`), keyless YouTube caption extraction with error handling, and Cloudinary PDF binary cleanup verified.
     - Story 2.4: ~500 token semantic chunker with anchor metadata (`pageNumber`, `timestampSeconds`), Qdrant `contextual_chunks_v1` upsert payload, and Neon `ready` state transition with `chunkCount` verified.
     - Story 2.5: `<SourceCard>` tactile states (`queued`, `indexing`, `ready`, `failed` with tooltip & `[Retry]`), icons, and delete confirmation modal with vector point cleanup verified.
     - Story 2.6: Non-blocking write to `telemetry_file_uploads` (< 15ms overhead) and midnight maintenance aggregation to `telemetry_daily_aggregates` verified.
   - *Disposition:* Accept as-is. As-built matches declared spec 100%.

### Diff-Scope Review

- **Module Boundaries:** Event payloads between API routes, Inngest triggers, extraction adapters, vector indexers, and UI components are strictly typed in `backend/src/shared-kernel/types.ts`.
- **Verification Gaps:** Zero gaps identified. All extractors, API routes, components, and telemetry services covered by unit test suite.

## Behavior Verification

- **Test Suite Execution:** Ran Vitest over 24 test suites (`npm test` / `vitest run`).
- **Results:** 128 tests passed, 0 failures.
- **Exercised Flows:**
  - Multi-modal extractor parsing (PDF page anchors, SRT/VTT regex, YouTube captions, text normalization).
  - Anchored markdown splitting & metadata attachment (`pageNumber`, `timestampSeconds`, `excerpt`).
  - Inngest function configuration (concurrency key/limit, retries, `onFailure` handler).
  - Source Card Neo-Brutalist status rendering and tooltip/retry interactions.
  - Upload Dialog 5-tab switching, client-side validation rules, and notebook quota warning.
  - Operational telemetry logging and midnight aggregation rollup.

## Previous-Retro Follow-Through

- *Status:* No previous retrospective exists for this project (Epic 2 is the first retrospective executed).

## Action Items

1. **Staging Multi-User Concurrency Load Test**
   - **ID:** `epic-2-retro-item-1-staging-concurrency-test`
   - **Action:** Run end-to-end integration test of live Inngest serverless endpoint under concurrent multi-user upload scenarios to validate real-world Queue latency.
   - **Owner:** Dev Team
   - **Status:** open
   - **Ref:** `_bmad-output/implementation-artifacts/epic-2-retro-09-07-2026.md`

2. **Telemetry Data Retention Purge Policy**
   - **ID:** `epic-2-retro-item-2-telemetry-retention-policy`
   - **Action:** Add 30-day automated cleanup for raw `telemetry_file_uploads` rows in midnight maintenance while retaining permanent `telemetry_daily_aggregates`.
   - **Owner:** Dev / Infrastructure Team
   - **Status:** open
   - **Ref:** `_bmad-output/implementation-artifacts/epic-2-retro-09-07-2026.md`

## Acceptance Verdict

- **Verdict:** `accepted`
- **Criteria:** `declared`
- **Evidence:** All 6 stories in Epic 2 are completed and marked `done` in `sprint-status.yaml`. All acceptance criteria across Stories 2.1–2.6 are verified by code inspection and 128 passing unit tests in Vitest. No pending stories or unresolved blocking findings remain.

## Open Questions

- None. Implementation, tests, and documentation are aligned.

## Assumptions

- None (Interactive mode run with verified evidence).
