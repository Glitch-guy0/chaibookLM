# chaiRAG

## Setup

Before running locally, provision the external services listed in `_bmad-output/planning-artifacts/external-tools-tracker.md` and fill in `.env.local` (see `.env.example` for required keys).
