import { U as ReasoningResult, V as ReasoningEffortLevel, W as ReasoningStrategy } from "./types-D7IBqJMJ.mjs";
//#region src/kairo/reasoning/simple-reasoning-strategy.d.ts
interface SimpleReasoningStrategyConfig {
  /** Strategy name surfaced in results. Default 'simple-reasoning'. */
  readonly name?: string;
  /** Effort level (affects ReasoningManager retry budget). Default 'LOW'. */
  readonly effortLevel?: ReasoningEffortLevel;
}
/**
 * SimpleReasoningStrategy — Kairo simple reasoning strategy template
 * (Story 7.3, FR-37).
 *
 * A deterministic, offline strategy: it synthesizes the provided context
 * lines into a single reasoning output without any LLM call. This satisfies
 * the `ReasoningStrategy` port and is wireable via import-and-inject into
 * `ReasoningManager` (swap-by-replacement, FR-14). Swap it for a richer
 * strategy (e.g. an LLM-backed one) via one-line instantiation.
 *
 * @example
 * ```ts
 * import { SimpleReasoningStrategy } from '@shikigami/kairo/reasoning';
 * import { ReasoningManager } from 'shikigami/reasoning';
 *
 * new ReasoningManager({ strategies: [new SimpleReasoningStrategy()] });
 * ```
 */
declare class SimpleReasoningStrategy implements ReasoningStrategy {
  readonly name: string;
  readonly effortLevel: ReasoningEffortLevel;
  constructor(config?: SimpleReasoningStrategyConfig);
  reason(query: string, context: string[]): Promise<ReasoningResult>;
}
declare namespace index_d_exports {
  export { SimpleReasoningStrategy, SimpleReasoningStrategyConfig };
}
//#endregion
export { SimpleReasoningStrategy as n, SimpleReasoningStrategyConfig as r, index_d_exports as t };
//# sourceMappingURL=index-CxpHMOLO.d.mts.map