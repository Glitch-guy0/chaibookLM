import { F as MemoryRetrieveResult, L as MemoryStoreInput, P as MemoryRetrieveInput, R as MemoryStoreResult, z as MemoryStrategy } from "./types-D7IBqJMJ.mjs";
//#region src/kairo/memory/simple-store.d.ts
interface SimpleStoreConfig {
  /** Strategy name surfaced in results. Default 'simple-store'. */
  readonly name?: string;
}
/**
 * SimpleStore — Kairo in-memory memory template (Story 7.1, FR-16a).
 *
 * Stores every entry in an internal array via `store()`; `retrieve()`
 * returns the ENTIRE array regardless of the query — no selective
 * retrieval, no embedding search, no ranking. This is the deliberately
 * simple batteries-included default; swap to a richer strategy via
 * one-line instantiation (FR-14).
 *
 * @example
 * ```ts
 * import { SimpleStore } from '@shikigami/kairo/memory';
 * import { MemoryManagerImpl } from 'shikigami/retrieval';
 *
 * new MemoryManagerImpl({ strategies: [new SimpleStore()] });
 * ```
 */
declare class SimpleStore implements MemoryStrategy {
  readonly name: string;
  private readonly entries;
  constructor(config?: SimpleStoreConfig);
  store(input: MemoryStoreInput): Promise<MemoryStoreResult>;
  retrieve(_input: MemoryRetrieveInput): Promise<MemoryRetrieveResult>;
}
declare namespace index_d_exports {
  export { SimpleStore, SimpleStoreConfig };
}
//#endregion
export { SimpleStore as n, SimpleStoreConfig as r, index_d_exports as t };
//# sourceMappingURL=index-C1-awIhu.d.mts.map