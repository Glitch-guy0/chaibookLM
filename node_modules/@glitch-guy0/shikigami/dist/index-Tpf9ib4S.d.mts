import { c as Guardrail, l as GuardrailResult } from "./types-D7IBqJMJ.mjs";
//#region src/kairo/guardrails/simple-input-guardrail.d.ts
interface SimpleInputGuardrailConfig {
  /** Guardrail name surfaced in block reasons. Default 'simple-input-guardrail'. */
  readonly name?: string;
  /** Human-readable description of what this guardrail blocks. */
  readonly description?: string;
  /**
   * Case-insensitive terms that block the prompt. Default ['politics'].
   * A prompt containing any term is blocked; otherwise it passes.
   */
  readonly blockedTerms?: string[];
}
/**
 * SimpleInputGuardrail — Kairo simple input guardrail template (Story 7.4,
 * FR-33). Block/allow only: it never modifies the prompt (FR-29). On a
 * blocked term it returns `{ allowed: false, reason }`; `GuardrailManager`
 * then throws `GuardrailError` and invokes hitRun exactly once (AD-5).
 *
 * @example
 * ```ts
 * import { SimpleInputGuardrail } from '@shikigami/kairo/guardrails';
 *
 * new SimpleInputGuardrail({ blockedTerms: ['politics', 'election'] });
 * ```
 */
declare class SimpleInputGuardrail implements Guardrail {
  readonly name: string;
  readonly description: string;
  private readonly blockedTerms;
  constructor(config?: SimpleInputGuardrailConfig);
  check(input: string): Promise<GuardrailResult>;
}
declare namespace index_d_exports {
  export { SimpleInputGuardrail, SimpleInputGuardrailConfig };
}
//#endregion
export { SimpleInputGuardrail as n, SimpleInputGuardrailConfig as r, index_d_exports as t };
//# sourceMappingURL=index-Tpf9ib4S.d.mts.map