// Template stub: GroundedAnswerReasoningStrategy
// Provides shikigami-compatible reasoning strategy that grounds answers in citations.
// Real implementation deferred to Epic 4 (chat + reasoning).

export class GroundedAnswerReasoningStrategy {
  async reason(_query: string, _context: unknown[]): Promise<string> {
    throw new Error('Not implemented');
  }
}